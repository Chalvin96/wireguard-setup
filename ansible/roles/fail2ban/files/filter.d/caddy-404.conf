[INCLUDES]
before = common.conf

[Definition]
# Matches excessive 404 responses in Caddy JSON access logs.
failregex = ^{"level":"info".*?"remote_ip":"<HOST>".*?"status":404[,}]
            ^{"level":"info".*?"status":404.*?"remote_ip":"<HOST>"

ignoreregex =

datepattern = "ts"\s*:\s*<EPOCH>
