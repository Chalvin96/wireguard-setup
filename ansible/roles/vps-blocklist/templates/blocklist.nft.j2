#!/usr/sbin/nft -f
# Managed by Ansible — do not edit manually

add table inet blocklist
add set inet blocklist banned { type ipv4_addr; flags timeout; }
add chain inet blocklist input { type filter hook input priority -10; policy accept; }
flush chain inet blocklist input
add rule inet blocklist input ip saddr @banned drop
